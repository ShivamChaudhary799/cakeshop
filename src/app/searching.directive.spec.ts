import { SearchingDirective } from './searching.directive';

describe('SearchingDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchingDirective();
    expect(directive).toBeTruthy();
  });
});
